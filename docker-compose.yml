services:
  flash-loan-agent:
    build:
      context: ./flash_loan_agent
      dockerfile: dockerfile
    container_name: flash-loan-agent
    ports:
      - "8000:8000"
    restart: unless-stopped 