services:
  flash-loan-agent:
    build:
      context: ./aarkham-chat-server
      dockerfile: dockerfile
    container_name: flash-loan-agent
    ports:
      - "8000:8000"
    restart: unless-stopped 